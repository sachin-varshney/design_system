import DehaatDesignSystem from './NativeDehaatDesignSystem';

export function multiply(a: number, b: number): number {
  return DehaatDesignSystem.multiply(a, b);
}

export { default as AppButton } from './components/AppButton';
